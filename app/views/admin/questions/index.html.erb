This test contains <%= Question.count %> questions<br/>
<%= link_to "Add New", new_admin_question_path %>
<hr>

<% Question.all.each do |question| %>
	<%= question.study_text %> <%= link_to "Edit", edit_admin_question_path( question ) %> <%= link_to "Delete", admin_question_path( question) , :method => "DELETE", :confirm => "Are you sure you would like to delete this question?" %><br/><br/>
<% end %>