User Email Hash / Response Type / Test Week / Trial / Study Text / Study Time / Response / Recall Reaction Time / Total Reaction Time / Correct / Keyword Hit Count / Keyword Miss Count / Keyword Hits / Keyword Misses / Total Keyword Count
<br/><br/>

<% @responses.each do |response| %>
    <%= response.study_user.email_hash %> /
	<%= response.response_type %> /
	<%= response.test_period %> /
	<%= response.trial %> /
	<%= response.question.study_text if response.question %> /
	<%= response.study_time %> /
	<%= response.response %> /
	<%= response.recall_reaction_time %> /
	<%= response.total_reaction_time %> /
	<%= response.correct %> /
	<%= response.keyword_hit_count %> /
	<%= response.keyword_miss_count %> /
	<%= response.keyword_hits %> /
	<%= response.keyword_misses %> /
	<%= response.keyword_total_count %> /<br/>
<% end %>

